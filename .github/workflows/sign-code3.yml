name: Code Signing Workflow

#on:
 # push:
  #  tags:
   #   - 'v*.*.*'

on: 
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

 #     uses: actions/setup-node@v2
  #    with:
   #     node-version: '14'

 